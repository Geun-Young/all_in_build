# Claude Code 프롬프트 — All-In Build 전체 구조 개편

---

```
올인빌드 앱 전체 구조를 아래와 같이 개편해줘.
50대 이상 사용자가 주로 쓰는 앱이니까 글씨 크기, 여백, 터치 영역을 크게 잡아줘.

---

## 1. 전체 구조 변경

### 기존 구조 (삭제)
- 대시보드
- 공사 현장
- 견적 관리
- 현장 일지  ← 메뉴에서 완전 제거
- 도면 설계

### 새 구조
- 메인 (공사 대시보드) → /
- 공사 상세 페이지 → /projects/[id]
  └── 탭: 공사현장 / 견적관리 / 도면설계

---

## 2. 사이드바/네비게이션 변경

Sidebar.tsx, MobileNav.tsx에서:
- "현장 일지" 메뉴 완전 제거
- "대시보드" → "공사 목록" 으로 텍스트 변경, 링크는 /
- "공사 현장", "견적 관리", "도면 설계" 메뉴 제거
  (이 기능들은 공사 상세 페이지 안으로 이동)

최종 사이드바:
- 🏠 공사 목록 → /
- (하단) All-In Build 로고

모바일 하단 탭도 동일하게 단순화.

---

## 3. 메인 페이지 (/) — 공사 대시보드

src/app/page.tsx 를 메인 공사 대시보드로 완전 교체.
(기존 /dashboard 내용을 / 로 이동, /dashboard는 / 로 리다이렉트)

### 상단 헤더
- 좌측: "공사 목록" 제목 (font-size: 22px, font-weight: 600)
- 우측: "+ 새 공사 등록" 버튼 (primary 네이비, 높이 48px, 글씨 16px)

### 검색바
- 전체 너비 검색 input
- placeholder: "공사명 또는 위치 검색..."
- 높이 52px, 글씨 16px
- 돋보기 아이콘

### 필터 탭
- 전체 / 진행중 / 완료 / 대기
- 탭 글씨 16px, 탭 높이 44px

### 공사 카드 목록
- 1열 리스트 형태 (카드형, 모바일/데스크탑 동일)
- 카드 높이 넉넉하게 (padding 20px 이상)
- 카드 내용:
  * 공사명 (font-size: 18px, font-weight: 600)
  * 시행청 + 위치 (font-size: 15px, color: #6b7280)
  * 기간 (font-size: 14px)
  * 상태 배지 (글씨 14px, padding 넉넉하게)
  * 우측에 ">" 화살표 아이콘
- 카드 클릭 → /projects/[id]
- 카드 간격 12px

### 빈 상태
공사 없을 때: 중앙에 아이콘 + "등록된 공사가 없습니다" (18px) + "+ 새 공사 등록" 버튼

---

## 4. 공사 상세 페이지 (/projects/[id])

기존 ProjectDetail.tsx를 완전 교체.

### 상단 헤더
- 뒤로가기 버튼 (← 아이콘, 터치 영역 44px 이상)
- 공사명 (font-size: 20px, font-weight: 600)
- 상태 배지

### 탭 네비게이션 (3개)
탭 글씨 16px, 탭 높이 52px, 탭 아이콘 + 텍스트

- 📸 공사현장
- 💰 견적관리
- 📐 도면설계

탭 스타일:
- 비활성: 텍스트 #6b7280
- 활성: 텍스트 #1e3a5f, 하단 보더 2px #1e3a5f
- 탭 전체 너비를 3등분

### 탭1: 공사현장
기존 /projects/[id] 의 작업기록 탭 내용 그대로 이동.
(사진, 날짜별 타임라인, + 작업 추가)

### 탭2: 견적관리
기존 /estimate/[id] 내용을 여기로 이동.
공사 id와 연동해서 해당 공사의 견적서 표시.
견적서 없으면 "+ 새 견적서 작성" 버튼.

### 탭3: 도면설계
기존 /blueprint 내용 그대로 이동.

---

## 5. 글씨 크기 전체 기준 (50대 이상 사용자)

tailwind.config.ts 에 fontSize 추가:
```
기존 Tailwind 기본값보다 전체적으로 크게:
- 본문 텍스트: 16px (기존 14px)
- 보조 텍스트: 15px (기존 12px)
- 소제목: 18px
- 제목: 22px
- 버튼 글씨: 16px
- 입력창 글씨: 16px (모바일에서 자동 확대 방지)
- 배지 글씨: 14px
```

globals.css 에 추가:
```css
/* 모바일 입력창 자동 확대 방지 */
input, textarea, select {
  font-size: 16px !important;
}

/* 전체 기본 글씨 크기 */
body {
  font-size: 16px;
}
```

---

## 6. 터치 영역 기준

모든 버튼, 탭, 링크의 최소 높이: 44px
아이콘 버튼 크기: 44px × 44px
카드 내 텍스트 줄간격: 1.7

---

## 7. 라우팅 정리

삭제할 페이지:
- src/app/dashboard/ → / 로 리다이렉트 추가
- src/app/site-log/ → 삭제
- src/app/estimate/page.tsx → / 로 리다이렉트 (견적은 공사 안으로)
- src/app/blueprint/page.tsx → / 로 리다이렉트 (도면은 공사 안으로)

유지할 페이지:
- src/app/page.tsx (메인 공사 목록)
- src/app/projects/[id]/page.tsx (공사 상세 - 탭 3개)

---

## 8. 더미 데이터 유지

기존 더미 데이터 그대로 유지.
Supabase 연결은 나중에 할 것.

---

## 마무리

완료 후:
git add .
git commit -m "feat: 전체 구조 개편 - 공사 중심 UX, 글씨 크기 확대"
git push origin main
```
