# 건설 견적 계산 로직 전체 정리
# All-In Build 견적 기능 구현용 참조 문서
# 출처: 금고동 상수도 이설공 D150 엑셀 (EST 프로그램 산출물)

---

## 1. 전체 계산 흐름 (5단계)

```
[1단계] 기초 단가 수집
  노임단가 + 자재단가 + 기계경비
        ↓
[2단계] 단가산출 (기계 1단위당 비용 계산)
  굴삭기 시간당 굴착량 → 터파기 1㎥당 단가
        ↓
[3단계] 일위대가 (공종 1단위당 종합 단가)
  인력(품셈) + 장비 + 재료 + 야간할증
        ↓
[4단계] 내역서 (수량 × 단가 = 금액)
  각 공종별 수량 입력 → 노무비/재료비/경비 분리 계산
        ↓
[5단계] 원가계산서 (간접비 + 이윤 + 세금)
  순공사비 → 간접노무비 → 각종 보험료 → 관리비 → 이윤 → 부가세 → 최종금액
```

---

## 2. 노임단가 (직종별 1인/일 임금)

실제 엑셀 기준값:

| 직종 | 단가(원/인) | 기준 |
|------|------------|------|
| 특별인부 | 224,490 | 2025년 하반기 |
| 건설기계운전사 | 279,824 | 2025년 하반기 |
| 보통인부 | 171,037 | 2025년 하반기 |
| 일반기계운전사 | 172,387 | 2025년 하반기 |
| 배관공(수도) | 237,446 | 2023년 하반기 |
| 화물차운전사 | 218,549 | 2023년 하반기 |
| 콘크리트공 | 271,064 | 2025년 하반기 |
| 포장공 | 270,747 | 2025년 하반기 |

> `인` 단위 = 1명이 하루 8시간 일하는 양
> `0.1인` = 1명이 48분 일하는 양

---

## 3. 일위대가 계산식 (공종별 실제 단가)

### 3-1. 기본 계산식

```
공종 단가 = Σ(구성요소 수량 × 단가) + 간접 비용(손료 등)
```

### 3-2. 실제 공종별 계산 예시

#### KP접합 Φ150mm (개소당)
```
배관공(수도)  0.1인  × 237,446 = 23,744원  [노무비]
보통인부      0.05인 × 171,037 =  8,551원  [노무비]
공구손료      노무비의 2%      =    645원  [경비]
─────────────────────────────────────────
합계 (주간)                     32,941원  ← 실제 엑셀 값 일치 ✅
```

#### KP접합 Φ150mm (야간) (개소당)
```
주간 단가                       32,941원
야간할증 = 노무비 × 87.5%
  노무비 합계 = 23,744 + 8,551 = 32,295원
  야간할증 = 32,295 × 0.875   = 28,258원
─────────────────────────────────────────
야간 합계  = 32,941 + 28,258  = 61,200원 ← 실제 엑셀 값 일치 ✅
```

#### 이탈방지접합 Φ150mm (개소당)
```
배관공(수도)  0.13인  × 237,446 = 30,868원 [노무비]
보통인부      0.065인 × 171,037 = 11,117원 [노무비]
공구손료      노무비의 2%       =    840원 [경비]
─────────────────────────────────────────
합계 (주간)                      42,824원 ← 실제 엑셀 값 일치 ✅
```

#### 이탈방지접합 Φ150mm (야간) (개소당)
```
주간 단가                        42,824원
노무비 합계 = 30,868 + 11,117  = 41,985원
야간할증    = 41,985 × 0.875   = 36,737원
─────────────────────────────────────────
야간 합계   = 42,824 + 36,737  = 79,561원 ← 실제 엑셀 값 일치 ✅
```

#### 터파기(야간) ㎥당
```
주간 터파기 단가(단가산출 기준) = 28,505원
  노무비: 26,565원 / 재료비: 749원 / 경비: 1,191원
야간할증 = 노무비 × 87.5%
  = 26,565 × 0.875 = 23,244원
─────────────────────────────────────────
야간 터파기 = 28,505 + 23,244 = 51,749원 ← 실제 엑셀 값 일치 ✅
```

#### 되메우기및다짐(야간) ㎥당
```
주간 단가 = 10,500원
  노무비: 9,504원 / 재료비: 469원 / 경비: 527원
야간할증 = 9,504 × 0.875 = 8,316원
─────────────────────────────────────────
야간 합계  = 10,500 + 8,316 = 18,816원 ← 실제 엑셀 값 일치 ✅
```

### 3-3. 공종별 실제 단가 전체 목록

실제 엑셀(일위대가총괄표)에서 추출한 값:

| 호표 | 공종명 | 규격 | 단위 | 단가(원) | 노무비 | 재료비 | 경비 |
|------|--------|------|------|---------|--------|--------|------|
| 제1호 | 터파기(야간) | 백호0.2㎥ | ㎥ | 51,749 | 49,809 | 749 | 1,191 |
| 제2호 | 되메우기및다짐(야간) | 백호0.2㎥ | ㎥ | 18,816 | 17,820 | 469 | 527 |
| 제3호 | 아스팔트포장절단(야간) | - | m | 3,257 | 2,559 | 665 | 33 |
| 제4호 | 아스콘포장깨기적재(야간) | 브레이커 | ㎥ | 62,326 | 52,921 | 3,403 | 6,002 |
| 제5호 | KP접합 | Φ150mm | 개소 | 32,941 | 32,296 | 645 | - |
| 제6호 | KP접합(야간) | Φ150mm | 개소 | 61,200 | 60,555 | 645 | - |
| 제7호 | 이탈방지접합 | Φ150mm | 개소 | 42,824 | 41,985 | 839 | - |
| 제8호 | 이탈방지접합(야간) | Φ150mm | 개소 | 79,561 | 78,722 | 839 | - |
| 제9호 | 제수밸브부설및접합(야간) | Φ150 | 기 | 128,714 | 118,950 | 4,999 | 4,765 |
| 제10호 | 이형관 부설 | Φ150mm,인력 | 개소 | 93,105 | 93,105 | - | - |
| 제11호 | 이형관 부설(야간) | Φ150mm,인력 | 개소 | 174,572 | 174,572 | - | - |
| 제12호 | 주철관 절단 | Φ150 | 개소 | 24,931 | 23,744 | 1,187 | - |
| 제13호 | 주철관 절단(야간) | Φ150 | 개소 | 45,708 | 44,521 | 1,187 | - |
| 제23호 | 교통통제및안전관리(야간) | 1인 기준 | 일 | 320,694 | 320,694 | - | - |
| 제24호 | 제수밸브조절(야간) | Φ300이하 | 개소 | 47,868 | 47,868 | - | - |
| 제25호 | 관세척공(신설,야간) | D=150mm | 개소 | 68,828 | 33,296 | 35,532 | - |
| 제26호 | 관세관공(폴리픽) | D=150~300mm | 회 | 18,290 | 17,758 | 532 | - |
| 제29호 | 라바콘설치및철거 | - | M | 1,784 | 684 | 1,100 | - |
| 제29호 | 라바콘설치및철거(야간) | - | M | 2,382 | 1,282 | 1,100 | - |

---

## 4. 내역서 계산식

### 4-1. 항목별 계산

```typescript
// 각 내역 항목 계산
const total = Math.round(unitPrice * quantity)
const labor = Math.round(unitPrice * (laborRatio / 100) * quantity)
const material = Math.round(unitPrice * (materialRatio / 100) * quantity)
const expense = total - labor - material
```

### 4-2. 실제 내역서 항목 (금고동 이설공)

| 공종 | 품명 | 수량 | 단위 | 단가(원) |
|------|------|------|------|---------|
| 토공 | 아스팔트포장절단(야간) | 16 | m | 3,257 |
| 토공 | 아스콘포장깨기적재(야간) | 1.6 | ㎥ | 62,326 |
| 토공 | 터파기(주간) | 12.6 | ㎥ | 28,505 |
| 토공 | 터파기(야간) | 30.1 | ㎥ | 51,749 |
| 토공 | 되메우기및다짐(주간) | 12.6 | ㎥ | 10,500 |
| 토공 | 되메우기및다짐(야간) | 30.1 | ㎥ | 18,816 |
| 토공 | 아스콘포장(기층,T=15) | 0.08 | a | 1,465,386 |
| 토공 | 아스콘포장(표층,t=5) | 0.08 | a | 2,265,444 |
| 배관공 | KP접합(주간) | 2 | 개소 | 32,941 |
| 배관공 | KP접합(야간) | 2 | 개소 | 61,200 |
| 배관공 | 이탈방지접합(주간) | 10 | 개소 | 42,824 |
| 배관공 | 이탈방지접합(야간) | 16 | 개소 | 79,561 |
| 배관공 | 제수밸브부설및접합(야간) | 2 | 기 | 128,714 |
| 배관공 | 이형관부설(주간) | 7 | 개소 | 93,105 |
| 배관공 | 이형관부설(야간) | 14 | 개소 | 174,572 |
| 배관공 | 주철관절단(주간) | 6 | 개소 | 24,931 |
| 배관공 | 주철관절단(야간) | 8 | 개소 | 45,708 |
| 구조물공 | 제수변보호실(야간) | 2 | 개소 | 1,225,576 |
| 부대공 | 교통통제및안전관리(야간) | 4 | 일 | 320,694 |
| 부대공 | 제수밸브조절(야간) | 4 | 개소 | 47,868 |
| 부대공 | 관세척공(야간) | 1 | 개소 | 68,828 |
| 부대공 | 라바콘설치및철거 | 10 | M | 1,784 |
| 부대공 | 라바콘설치및철거(야간) | 20 | M | 2,382 |
| 사급자재 | 아스콘(기층용) | 2.8 | Ton | 79,000 |

---

## 5. 노무비/재료비/경비 비율 계산 (핵심!)

### 현재 코드 문제점
표준시장단가는 `unit_price`와 `labor_ratio`만 있음.
재료비/경비를 어떻게 분리할지 명확한 기준이 없어서 계산이 틀림.

### 정확한 계산 방법

```typescript
// 방법 1: 노무비율만 알 때 (표준시장단가 DB 기준)
function calcAmounts(unitPrice: number, laborRatio: number, quantity: number) {
  const total = Math.round(unitPrice * quantity)
  const labor = Math.round(total * (laborRatio / 100))
  // 재료비와 경비는 나머지를 8:2로 분리 (건설업 일반 관행)
  const rest = total - labor
  const material = Math.round(rest * 0.8)
  const expense = rest - material
  return { labor, material, expense, total }
}

// 방법 2: 노무비/재료비/경비가 별도로 있을 때 (일위대가 DB 기준)
function calcAmountsExact(
  laborPrice: number,
  materialPrice: number,
  expensePrice: number,
  quantity: number
) {
  return {
    labor:    Math.round(laborPrice * quantity),
    material: Math.round(materialPrice * quantity),
    expense:  Math.round(expensePrice * quantity),
    total:    Math.round((laborPrice + materialPrice + expensePrice) * quantity)
  }
}
```

### 야간할증 계산식 (핵심)

```typescript
// 야간 = 주간 단가 + 노무비 × 87.5%
// 표준시장단가에 야간(_N) 별도 저장되어 있으므로
// DB에서 is_night = true 항목 그대로 사용하면 됨

// 만약 주간 단가에서 야간 계산해야 할 때:
function calcNightPrice(dayPrice: number, laborRatio: number): number {
  const laborAmount = dayPrice * (laborRatio / 100)
  const nightSurcharge = Math.round(laborAmount * 0.875)
  return dayPrice + nightSurcharge
}
```

---

## 6. 원가계산서 계산식 (간접비 전체)

실제 엑셀 원가계산서에서 추출한 정확한 요율과 적용 기준:

### 변수 정의
```
A = 재료비 합계 (직접재료비 + 간접재료비)
4 = 직접노무비
B = 노무비 합계 (직접 + 간접)
6 = 산출경비
```

### 순서대로 계산

```typescript
function calcFinalAmount(directMaterial: number, directLabor: number, directExpense: number) {
  // A: 재료비 소계
  const A = directMaterial

  // 4: 직접노무비
  const labor_direct = directLabor

  // 5: 간접노무비 = 직접노무비 × 16.5%
  const labor_indirect = Math.round(labor_direct * 0.165)

  // B: 노무비 소계
  const B = labor_direct + labor_indirect

  // 6: 산출경비 (직접경비)
  const expense_direct = directExpense

  // 각종 보험료 계산
  const 산재보험료        = Math.round(B * 0.0356)
  const 고용보험료        = Math.round(B * 0.0101)
  const 건강보험료        = Math.round(labor_direct * 0.03595)   // 직접노무비 기준
  const 연금보험료        = Math.round(labor_direct * 0.0475)    // 직접노무비 기준
  const 노인장기요양보험료  = Math.round(건강보험료 * 0.1314)      // 건강보험료 기준
  const 퇴직공제부금비     = Math.round(labor_direct * 0.023)    // 직접노무비 기준
  const 건설기계대여금      = Math.round((A + labor_direct + expense_direct) * 0.004)
  const 산업안전보건관리비  = Math.round((A + labor_direct + expense_direct) * 0.0315)
  const 환경보전비         = Math.round((A + labor_direct + expense_direct) * 0.008)
  const 하도급대금지급보증  = Math.round((A + labor_direct + expense_direct) * 0.00081)
  const 석면분담금         = Math.round(B * 0.00006)
  const 임금채권부담금      = Math.round(B * 0.0009)
  const 기타경비           = Math.round((A + B) * 0.052)

  // C: 경비 소계
  const C = expense_direct + 산재보험료 + 고용보험료 + 건강보험료
    + 연금보험료 + 노인장기요양보험료 + 퇴직공제부금비
    + 건설기계대여금 + 산업안전보건관리비 + 환경보전비
    + 하도급대금지급보증 + 석면분담금 + 임금채권부담금 + 기타경비

  // D: 순공사원가
  const D = A + B + C

  // E: 일반관리비 = 순공사원가 × 8%
  const E = Math.round(D * 0.08)

  // F: 이윤 = (노무비 + 경비 + 관리비) × 15%
  const F = Math.round((B + C + E) * 0.15)

  // G: 총원가
  const G = D + E + F

  // H: 부가가치세 = 10%
  const H = Math.round(G * 0.10)

  // I: 최종 도급액
  const I = G + H

  return {
    재료비: A,
    직접노무비: labor_direct,
    간접노무비: labor_indirect,
    노무비소계: B,
    경비소계: C,
    순공사원가: D,
    일반관리비: E,
    이윤: F,
    총원가: G,
    부가가치세: H,
    최종도급액: I,
    상세: {
      산재보험료, 고용보험료, 건강보험료, 연금보험료,
      노인장기요양보험료, 퇴직공제부금비, 건설기계대여금,
      산업안전보건관리비, 환경보전비, 하도급대금지급보증,
      석면분담금, 임금채권부담금, 기타경비
    }
  }
}
```

---

## 7. All-In Build 구현 시 주의사항

### 현재 코드에서 틀릴 수 있는 부분

1. **노무비/재료비/경비 분리**: 표준시장단가 DB에는 `labor_ratio`만 있음.
   - 재료비 = (단가 - 노무비) × 0.8 로 추정
   - 경비 = (단가 - 노무비) × 0.2 로 추정
   - 또는 별도 컬럼(`material_ratio`, `expense_ratio`) 추가 권장

2. **야간할증**: DB에 야간(_N) 항목이 별도로 저장되어 있으므로 주간/야간 토글로 다른 항목을 조회해야 함. 야간 체크박스는 불필요.

3. **원가계산서 보험료 계산 기준**:
   - 산재/고용보험료 → **B(노무비 소계)** 기준
   - 건강/연금/퇴직공제 → **직접노무비(4)** 기준
   - 건설기계대여금/산업안전/환경보전 → **(A + 직접노무비 + 직접경비)** 기준
   - 기타경비 → **(A + B)** 기준
   - 이윤 → **(노무비 + 경비 + 관리비)** 기준, 재료비 제외!

4. **반올림**: 모든 금액은 `Math.round()`로 원 단위 반올림

5. **수량 소수점**: 수량은 소수점 3자리까지 허용 (예: 1.6㎥, 0.08a, 0.065인)

### Supabase 테이블 개선 권장

```sql
-- standard_prices 테이블에 컬럼 추가 권장
ALTER TABLE standard_prices
ADD COLUMN material_ratio DECIMAL(5,2) DEFAULT 0,
ADD COLUMN expense_ratio DECIMAL(5,2) DEFAULT 0;

-- 예: KP접합 Φ150mm
-- unit_price: 32941, labor_ratio: 98.0, material_ratio: 1.96, expense_ratio: 0.04
-- (32,296 / 32,941 = 98%, 645 / 32,941 = 1.96%)
```

---

## 8. 구현 우선순위

```
1. 내역서 계산 수정
   - 노무비: unit_price × labor_ratio × quantity
   - 재료비: unit_price × (1 - labor_ratio) × 0.8 × quantity
   - 경비:   unit_price × (1 - labor_ratio) × 0.2 × quantity
   - 합계:   unit_price × quantity

2. 원가계산서 페이지 신규 구현
   - 내역서 합계(노무비/재료비/경비) 입력
   - 위 calcFinalAmount() 함수 적용
   - 항목별 금액 자동 계산 표시
   - 최종 도급액 출력

3. 엑셀 출력 개선
   - 내역서 시트: 공종별 그룹 + 소계 + 합계
   - 원가계산서 시트: 간접비 상세 포함
```
